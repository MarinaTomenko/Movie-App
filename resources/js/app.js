import './bootstrap';
import PerfectScrollbar from 'perfect-scrollbar';
import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();


window.PerfectScrollbar = PerfectScrollbar; // Делаем PerfectScrollbar глобальным